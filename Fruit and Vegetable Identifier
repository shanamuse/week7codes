For example:

Input	Result
apple
Please input the item to identify:
apple is fruit.





#include <stdio.h>
#include <string.h>

int main() {
    char input[20]; // Assuming input won't exceed 20 characters

    // List of known fruits and vegetables
    char *fruits[] = {"apple", "banana", "orange", "melon", "tomato"};
    char *vegetables[] = {"broccoli", "carrot", "onion", "potato", "spinach"};

    printf("Please input the item to identify:\n");

    while (1) {
        scanf("%s", input);

        // Check if input matches any known fruit
        for (int i = 0; i < 5; i++) {
            if (strcmp(input, fruits[i]) == 0) {
                printf("%s is fruit.\n", input);
                return 0;
            }
        }

        // Check if input matches any known vegetable
        for (int i = 0; i < 5; i++) {
            if (strcmp(input, vegetables[i]) == 0) {
                printf("%s is vegetable.\n", input);
                return 0;
            }
        }

        // If input does not match any known fruit or vegetable
        printf("%s is not a fruit or vegetable, try again:\n", input);
    }

    return 0;
}
